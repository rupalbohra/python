# Creating a tuple using()
t = (1, 3, 4, 5, 1, 3, 4, 2, 1, 1, 1)

# print(t.count(1))  # will count the number of elements in the tuple
# print(t.index(1))   # will tell the index of the first occurence of an element
